export const handleDictionaryTilesOpen = (setModalContent, setOpen) => {
  setModalContent("dictionaryTiles");
  setOpen(true);
};

export const handleColorSchemeOpen = (setModalContent, setOpen) => {
  setModalContent("colorScheme");
  setOpen(true);
};

export const handleRecentGamesOpen = (setModalContent, setOpen) => {
  setModalContent("recentGames");
  setOpen(true);
};

export const handleGamesHistoryOpen = (setModalContent, setOpen) => {
  setModalContent("gamesHistory");
  setOpen(true);
}; 